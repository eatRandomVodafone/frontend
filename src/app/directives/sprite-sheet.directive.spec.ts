import { SpriteSheetDirective } from './sprite-sheet.directive';

describe('SpriteSheetDirective', () => {
  it('should create an instance', () => {
    const directive = new SpriteSheetDirective();
    expect(directive).toBeTruthy();
  });
});
